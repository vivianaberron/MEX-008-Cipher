window.cipher = {
  encode: () => {}
  // ... 
  //let cipher = (string, number) => {
   // let box = ''; //Contiene la palabra a cifrar
    //for (let i = 0; i< string.length;i++){
    //  let newAscii = string.charCodeAt(i);
     // let cipherAscii = (newAscii-65+number)%26+65;
     // let cipherLetter = (String.fromCharCode (cipherAscii));
     // box = box + cipherLetter;
   // }
  //return box
 // }
 // cipher('ABC', 1)
// };
